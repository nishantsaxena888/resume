# Current React/Options Task Definition
1. Use `App.tsx` to ping `http://localhost:8000/entities/users/options` on Login.
2. Render those JSON keys as input fields on the screen automatically. 
3. Set CORS policy on FastAPI so it allows localhost:5173 to fetch data safely.
4. Commit step to Github so code is permanently stored.
